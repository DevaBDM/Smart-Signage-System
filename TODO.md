# UPDATE

1. post identification number displayed on the screen like a watermark, so that post can be find from the feeds or the admin and creator can use this id to find the post for update
   video.
2. One creator can manage multiple groups

# ADD

1. An emergency button or connected to emergency aler system that will make the system on emergency mode, where the raspberry pi will clear everything and starts displaying emergency informations stored in it for emergency cases.
2. Storing post seen using arduino motion or viwer detection to store which signage on display got viewer for statical analyse of the posts in signage
   - Statstical view of how much the post got view or like screen time of the post when the screen is off the no screen time when the sensor detect presence of viewer it will enable display so this will be recorded until it goes off, so each post will have a screen time data,
3. Integration with camera or live stream data, like when there is no post on signage or sometimes it should show the camera feed it got from someplace or if there is a meeting or a party to display it will live feed that.
4. Markdown desingning helper or guide toolbar like setting head 1 for selected
   line, lists, and other markdown formats cabable of putting them on toolbar
   - AI ask field to ask ai to create a format like table or some markdown (it
     should have the link and api setup) the ai can be a local model or a api like
     the gemini, and have prompt setup, those are in config so can be edited
     easly, this is should be in backend. the creator will ask him to add a table
     using this data or like specified the column and row, or ask him does this
     markdown support this,
5. If the server disconnected from the raspberry pi for more that 3 days, it purge everything and displays a disconnection timeout image stored locally.
6. A feed summary or Quastion AI that will answer user quation about the post using the posts, local AI support with open api compatibile configuration

# Fix
