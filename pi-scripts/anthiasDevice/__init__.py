"""Smart Signage — shared Anthias-based device agent package.

This package (anthiasDevice/) contains the canonical implementation for Pi
devices running Anthias. Per-device overrides are provided via a thin
`config.py` in the device's own folder.
"""
