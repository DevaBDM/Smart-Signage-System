"""Smart Signage — shared Anthias-based device agent package.

This package contains the canonical implementation for Pi devices running
Anthias (Device1 / Device2 style). Per-device overrides are provided via a
thin `config.py` in the device's own folder.
"""
